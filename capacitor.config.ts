import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.playstation.k',
  appName: 'playstation-lounge-dashboard',
  webDir: 'dist/playstation-lounge-dashboard/browser'
};

export default config;
